@inherits LayoutComponentBase

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4">
            @*<input type="checkbox" id="accumulation" name="accumulation" value="accumulation" onchange="toggleControls(event)" />
            <label class="label" for="accumulation">Accumulation</label>
            <input type="checkbox" id="accumulation" name="accumulationtypes" value="accumulationtypes" onchange="toggleControls(event)" />
            <label class="label" for="accumulationtypes">AccumulationTypes</label>*@
            <a href="https://docs.microsoft.com/aspnet/" target="_blank">About</a>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>
