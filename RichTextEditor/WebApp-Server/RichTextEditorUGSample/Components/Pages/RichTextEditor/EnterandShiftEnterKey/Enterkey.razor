<h5>Enter key customization</h5>

@page "/RichTextEditor/EnterandShiftEnterKey/Enterkey"

@using Syncfusion.Blazor.RichTextEditor

<br /><h6>Enter key as DIV tag</h6><br />

<SfRichTextEditor EnterKey="EnterKeyTag.DIV">
    <div>In Rich text Editor, the <kbd>Enter</kbd> key and <kbd>Shift</kbd> + <kbd>Enter</kbd> key actions can be customized using the EnterKey and ShiftEnterKey properties.</div>
</SfRichTextEditor>

<br /><h6>Enter key as P tag</h6><br />

<SfRichTextEditor EnterKey="EnterKeyTag.P">
    <div>In Rich text Editor, the <kbd>Enter</kbd> key and <kbd>Shift</kbd> + <kbd>Enter</kbd> key actions can be customized using the EnterKey and ShiftEnterKey properties.</div>
</SfRichTextEditor>

<br /><h6>Enter key as BR tag</h6><br />

<SfRichTextEditor EnterKey="EnterKeyTag.BR">
    <div>In Rich text Editor, the <kbd>Enter</kbd> key and <kbd>Shift</kbd> + <kbd>Enter</kbd> key actions can be customized using the EnterKey and ShiftEnterKey properties.</div>
</SfRichTextEditor>